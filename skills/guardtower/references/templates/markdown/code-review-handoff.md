<!--
  Code-Review Finding Handoff — MARKDOWN SKELETON (plain portable GFM)
  ------------------------------------------------------------------------------------------------
  guardtower's report, on EVERY run: the reconciled findings laid out per-finding with current code,
  a proposed fix, and why it works, plus the run's scope. Plain GitHub-Flavored Markdown — no styling,
  no build step, no render. There is no other report format (SKILL.md step 8).

  TO USE:
    1. Fill this file and write it to the run directory as `report.md`
       (`.guardtower/{date}-{run-name}/report.md`, see SKILL.md **Run artifacts**) in one write; the
       human moves it onward to a PR or wiki.
    2. Fill the cover line and "How to read this", then duplicate the "### Finding" block once per
       reconciled finding, grouped into themes most-actionable first.
    3. For EACH finding, fill all FOUR required parts, in this order — **Evidence**, **Current code**,
       **Proposed fix**, **Why this fixes it**. None is optional; a finding you cannot give evidence
       for is not a finding — drop it. Only the Proof block (and any type-specific block) is optional.
    4. Type-specific views (security, concurrency, data-safety, api-contract) are folded in HERE as
       extra labeled subsections under the finding (e.g. an "Exploit" block for security).
    5. Always fill "Review scope". On a clean run (no findings), drop Contents and the Theme blocks
       and keep the "No findings" section instead.
    6. Keep code as ```fenced blocks with a language hint, and any mermaid as ```mermaid fences (not
       images). This is the reader's document — no "generated by" line, no tool/skill attribution.
       guardtower is report-only: the "Proposed fix" blocks describe a change, they are never applied.
  ------------------------------------------------------------------------------------------------
-->

# <Target> — Code Review

> <N findings across M facets, each with current code, a proposed fix, and why it works.>

## Contents

<!-- One line per finding, in order. Fill after the findings are written. -->

- **Theme A — <Theme A title>**
  - [<A1> · <severity> — <short finding title>](#a1--severity--short-finding-title)

## How to read this

Findings are grouped into themes, most-actionable first. Each finding gives the **evidence** that it
is real, the **current code**, a **proposed fix**, and **why this fixes it**; some also carry a
proving test. Severity and confidence are noted on each finding's heading.

---

## Theme A — <Theme A title>

<One-line deck for the theme.>

### <A1> · <severity> — <short finding title>

`<file path : line>` · confidence: <high/medium/low>

<One or two sentences: what the finding is and why it matters.>

<!-- REQUIRED — Evidence: the concrete, checkable facts from THIS codebase that show the defect is
     real — cited `file:line`, quoted verbatim. E.g. where untrusted input enters and the line it
     reaches, the caller that retries, the schema column's type, the existing helper that already
     does this, or an input that triggers it. Not a restatement of the claim. -->
**Evidence**

- `<file path : line>` — <what this line shows>

```php
<the cited code, verbatim>
```

**Current code**

```php
<the code as it stands, the part the fix changes>
```

**Proposed fix**

```php
<the changed code>
```

**Why this fixes it**

<Why the proposed change defuses the finding.>

<!-- OPTIONAL — a type-specific block, e.g. for security: -->
<!--
**Exploit — how it is reached**

```http
<the request / input that triggers it>
```
-->

<!-- OPTIONAL — Proof: a test that passes today. Delete if the finding carries no proving test. -->
**Proof — this test passes today**

```bash
<how to run the proving test>
```

<!-- Duplicate the "### <finding>" block above for each remaining finding. -->

<!-- CLEAN RUN ONLY — replaces Contents and every Theme block when there are no findings. -->
<!--
## No findings

<One or two sentences: what the change does and why no selected facet found a defect in it.>
-->

---

## Review scope

- **Target:** <PR / branch / path / working diff, and what the change does in one line>
- **Effort:** <low | medium | high | max> (`top_n` <n>, `floor` <floor>) — <chosen by the human | defaulted to the recommendation: reason>
- **Tenancy / stack:** <tenancy verdict> · <stack set, or none>
- **Facets run:** <each selected facet, and for each opt-in or core-when-present facet the phrase from the change that selected it>
- **Facets not run:** <each facet left out, with why>

| Facet | Relevance | Findings | Held back |
|---|---|---|---|
| <facet> | <reviewed / self-skipped: reason> | <n> | <dropped count, or —> |

<!-- If any facet held findings back, say so in words, e.g. "Security: 3 more above the floor —
     re-run at a higher effort to see them." -->

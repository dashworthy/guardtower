<!--
  Code-Review Finding Handoff — MARKDOWN SKELETON (plain portable GFM)
  ------------------------------------------------------------------------------------------------
  A richer, shareable form of guardtower's reconciled report: the same findings the review produces,
  laid out per-finding with current code, a proposed fix, and why it works. Plain GitHub-Flavored
  Markdown — no styling, no build step, no render, no run directory. You copy this file, fill it, and
  you are done. Use it when the reconciled report is going to a person or a PR/wiki rather than
  staying as the terse default report; the default one-file report in SKILL.md step 6 is enough for a
  quick local read.

  TO USE:
    1. Copy this file to where the handoff belongs (a repo path, a PR description, or a wiki page).
    2. Fill the cover line and "How to read this", then duplicate the "### Finding" block once per
       reconciled finding, grouped into themes most-actionable first.
    3. For EACH finding, keep the three scaffold labels — **Current code**, **Proposed fix**, and
       **Why this fixes it**. Drop the OPTIONAL Evidence / Proof blocks a finding does not need.
    4. Type-specific views (security, concurrency, data-safety, api-contract) are folded in HERE as
       extra labeled subsections under the finding (e.g. an "Exploit" block for security).
    5. Keep code as ```fenced blocks with a language hint, and any mermaid as ```mermaid fences (not
       images). This is the reader's document — no "generated by" line, no tool/skill attribution.
       guardtower is report-only: the "Proposed fix" blocks describe a change, they are never applied.
  ------------------------------------------------------------------------------------------------
-->

# <Target> — Code Review

> <N findings across M facets, each with current code, a proposed fix, and why it works.>

## Contents

<!-- One line per finding, in order. Fill after the findings are written. -->

- **Theme A — <Theme A title>**
  - [<A1> · <severity> — <short finding title>](#a1--severity--short-finding-title)

## How to read this

Findings are grouped into themes, most-actionable first. Each finding states the **current code**, a
**proposed fix**, and **why this fixes it**; some also carry evidence from the codebase and a proving
test. Severity and confidence are noted on each finding's heading.

---

## Theme A — <Theme A title>

<One-line deck for the theme.>

### <A1> · <severity> — <short finding title>

`<file path : line>` · confidence: <high/medium/low>

<One or two sentences: what the finding is and why it matters.>

<!-- OPTIONAL — Evidence: the real code that handles it today. Delete if not needed. -->
**Evidence — the real code that handles it**

```php
<the current handling code, verbatim>
```

**Current code**

```php
<the code as it stands, the part the fix changes>
```

**Proposed fix**

```php
<the changed code>
```

**Why this fixes it**

<Why the proposed change defuses the finding.>

<!-- OPTIONAL — a type-specific block, e.g. for security: -->
<!--
**Exploit — how it is reached**

```http
<the request / input that triggers it>
```
-->

<!-- OPTIONAL — Proof: a test that passes today. Delete if the finding carries no proving test. -->
**Proof — this test passes today**

```bash
<how to run the proving test>
```

<!-- Duplicate the "### <finding>" block above for each remaining finding. -->
